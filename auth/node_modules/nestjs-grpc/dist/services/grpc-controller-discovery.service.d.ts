import { OnModuleInit } from '@nestjs/common';
import { DiscoveryService, MetadataScanner, Reflector } from '@nestjs/core';
import { GrpcRegistryService } from './grpc-registry.service';
export declare class GrpcControllerDiscoveryService implements OnModuleInit {
    private readonly discoveryService;
    private readonly metadataScanner;
    private readonly reflector;
    private readonly registryService;
    private readonly logger;
    constructor(discoveryService: DiscoveryService, metadataScanner: MetadataScanner, reflector: Reflector, registryService: GrpcRegistryService);
    onModuleInit(): void;
    private discoverAndRegisterControllers;
    private registerController;
}
