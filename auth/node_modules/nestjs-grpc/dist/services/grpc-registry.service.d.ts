import { OnModuleInit } from '@nestjs/common';
import { ControllerMetadata } from '../interfaces';
import { GrpcProviderService } from './grpc-provider.service';
export declare class GrpcRegistryService implements OnModuleInit {
    private readonly providerService;
    private readonly logger;
    private readonly pendingRegistrations;
    private isProcessingRegistrations;
    constructor(providerService: GrpcProviderService);
    onModuleInit(): void;
    registerController(serviceName: string, instance: any, metadata: ControllerMetadata): void;
    getRegisteredControllers(): Map<string, {
        instance: any;
        metadata: ControllerMetadata;
    }>;
    isControllerRegistered(serviceName: string): boolean;
    private processPendingRegistrations;
    private waitForProviderReady;
}
