apiVersion: v1
kind: Service
metadata:
  name: kong
spec:
  type: NodePort
  selector:
    app: kong
  ports:
    - name: http
      port: 8000
      nodePort: 30080
    - name: admin
      port: 8001
      nodePort: 30081

