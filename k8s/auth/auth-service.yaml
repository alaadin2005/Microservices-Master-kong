apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 9001
      targetPort: 9001

    - name: grpc
      port: 50051
      targetPort: 50051

