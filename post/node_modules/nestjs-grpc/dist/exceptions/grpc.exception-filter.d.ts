import { ArgumentsHost, RpcExceptionFilter } from '@nestjs/common';
import { Observable } from 'rxjs';
import type { GrpcExceptionFilterOptions } from '../interfaces';
export declare class GrpcExceptionFilter implements RpcExceptionFilter<any> {
    private readonly logger;
    private readonly options;
    constructor(options?: GrpcExceptionFilterOptions);
    catch(exception: any, _host: ArgumentsHost): Observable<any>;
    private normalizeError;
    private logError;
    private truncateMessage;
}
