import { OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { Observable } from 'rxjs';
import { GrpcOptions, GrpcClientOptions } from '../interfaces';
import { GrpcProtoService } from './grpc-proto.service';
export declare class GrpcClientService implements OnModuleInit, OnModuleDestroy {
    private readonly options;
    private readonly protoService;
    private readonly logger;
    private readonly clients;
    private readonly activeStreams;
    private cleanupInterval?;
    private readonly CLIENT_CACHE_TTL;
    private readonly CLEANUP_INTERVAL;
    constructor(options: GrpcOptions, protoService: GrpcProtoService);
    onModuleInit(): void;
    onModuleDestroy(): void;
    create<T>(serviceName: string, options?: Partial<GrpcClientOptions>): Promise<T>;
    call<TRequest, TResponse>(serviceName: string, methodName: string, request: TRequest, options?: Partial<GrpcClientOptions>): Promise<TResponse>;
    serverStream<TRequest, TResponse>(serviceName: string, methodName: string, request: TRequest, options?: Partial<GrpcClientOptions>): Observable<TResponse>;
    clientStream<TRequest, TResponse>(serviceName: string, methodName: string, request: Observable<TRequest>, options?: Partial<GrpcClientOptions>): Promise<TResponse>;
    bidiStream<TRequest, TResponse>(serviceName: string, methodName: string, request: Observable<TRequest>, options?: Partial<GrpcClientOptions>): Observable<TResponse>;
    private cleanupStaleClients;
    private executeWithRetry;
    private getClientKey;
    private getConfigHash;
    private mergeClientOptions;
    private getServiceConstructor;
    private getAvailableServiceNames;
    private findServicePath;
    private createClient;
    private callUnaryMethod;
    private validateMethod;
}
